import { GameScene } from "../scenes/GameScene";
import { Modal } from "./Modal";
import { RoundRectangle } from "./RoundRectangle";

export class ItemDetailsModal extends Modal {
	constructor(scene: GameScene) {
		super(scene);
	}
}
